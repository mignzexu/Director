# 架构 —— Agent Extensions 实现全景

> 本文是项目**已实现能力的完整清单**（截至 2026-09-06，装备阶段 Phase 1–10
> + 12 收官完成）。定位与不变量见《初衷.md》；各层设计依据见《DPN-AOR_最新版统一
> 架构蓝图.md》与路线图各 Phase 完成块。本文随每个 Phase 交付更新。

```
opencode（宿主）
   │  唯一接口：.opencode/plugins/agent-extensions.ts     ← L1 Extension 入口（永不膨胀）
   ▼
.extensions/src/adapters/opencode/index.ts                ← L2 静态注册表（一行登记一个插件）
   └── src/plugins/<id>/                                  ← 自包含插件文件夹
       （开发硬约束见：.extensions/src/plugins/README.md）
       └── runtime-ledger                                 ← 当前唯一活跃插件
           └── src/runtime/                               ← DPN-AOR Core Runtime（宿主无关）
```

------------------------------------------------------------------------

# 0. 依赖分层（独立性审计结论，2026-09-06）

| 层 | 范围 | 依赖 | 内容 |
|---|---|---|---|
| **A 固有层** | `src/runtime` + `src/state` + `src/core` | **仅 node 内置 + 相对导入** | 28 种事件、全部规则（事实/证据/计划/Solver/Director/Memory/Research/恢复）、账本与快照——系统 100% 的制度价值 |
| **B 插件层** | `src/plugins` | 自有契约 + zod | 两个插件：runtime-ledger（15 工具装配）+ flight-recorder（宿主事件黑匣子）；宿主耦合收口在 **ToolPort 三字段**（agent/sessionID/directory） |
| **C 适配层** | `src/adapters` + `.opencode` | 宿主 SDK（唯一允许落点） | ToolContext→ToolPort 映射、L1 薄入口、agent 定义、权限键 |

## 0.6 完成门禁强化（调试阶段第 2 项，2026-09-06——装灯不装锁）

- **设计哲学**：结构性发癫（伪造/越权/死循环）已被现有门禁拦截；本次消灭
  的是"幻觉式交卷"的藏身之处——**发癫不用拦，让它无处可藏**，判定归智能
- **完成档案 executionTrail**：completed（含 solver success 联动）时定格
  active 窗口（startedAt → 完成时刻）内黑匣子记录的宿主工具执行统计
  （count + 工具分布）——零动作交卷自证其伪
- **完成分级 completionKind**：witnessed（亲证完成）| attested（声称完成，
  合法存在——文档类/人审类任务）——planHealth 拆分 witnessedDone/
  attestedDone；**PlanNode 新增 startedAt**（started 时定格，窗口起点）
- **Director 仪表盘**：tick 带出 attestedDone 清单（含 trailCount——零动作
  交卷自动发光）；处理归教义（追问/要求补 run:）
- **飞行记录仪配套修正**：verify-run 从过滤列表移出——它是验证发生的核心
  执行证据（亲证完成的档案必须有它）

## 0.5 flight-recorder 插件（调试阶段第 1 项，2026-09-06）

- **定位：黑匣子不是事实机**——宿主工具执行（bash/edit…）自动记为
  `action.executed`（只记"发生过什么"），不下事实结论（fact 空间保持严格）
- **数据源**：宿主 event 流 `message.part.updated`（part.type=tool 且
  status ∈ completed/error）——L2 扇出的第一个真实消费者
- **防御式解析**：事件形状是宿主细节、版本敏感——形状不对静默跳过；
  过滤自有域工具（fact-/plan-/solver-/director-/verify-/memory-/research-）
  避免刷自己的屏
- **宿主实证**：deepseek flash 只被要求跑 bash（未要求记账）→ 账本自动
  出现 `action.executed`（source=flight-recorder）——"记账是自愿的"破局
- **L2 首次双插件**：事件扇出故障隔离语义在真实多插件下运行；
  registry 断言更新为双插件

- **ToolPort 契约**：工具 execute 的宿主面 = 三字段（`plugins/types.ts`）；
  opencode 的 ToolContext 由 L2 适配器映射，宿主 SDK 类型不外泄
- **边界由 `test/boundary.test.ts` 锁定**（五项断言，独立性是制度不是愿望）
- **换 coding CLI = 写一个新适配器**（如 MCP：zod→JSON Schema 转换为唯一
  技术点），A/B 层零改动；评估文档 §7 的"平台扩展下限"由此成为纯机械工作

# 1. 宿主层（opencode 侧）

- **L1 入口**：`.opencode/plugins/agent-extensions.ts` —— 唯一的
  opencode 侧文件（<40 行硬约束，测试钉死），只做路由不承载业务逻辑
- **私有 Agent**：`.opencode/agents/private.md` —— primary 模式，
  opencode 原生 `bash` 执行（沙箱已归档），9 个插件工具显式授权
  （fact/plan/solver/director 全系 `allow`；`external_directory`/
  `webfetch`/`websearch` deny）

# 2. L2 聚合器与插件框架

- **静态注册表**：`src/adapters/opencode/index.ts` —— 一行登记一个插件，
  当前：`[runtimeLedgerExtension]`
- **聚合器语义**（`composeExtensionHooks`，测试覆盖）：
  - `setup` 失败 → 记日志、不致命、不阻断其他插件
  - `onEvent` 抛错 → 记日志、故障隔离
  - 工具 id 冲突 → **启动即抛错**（配置错误必须响亮）
- **插件约定**：`src/plugins/README.md` —— 自包含文件夹、依赖白名单
  （`../../core|state|runtime/*`、`@opencode-ai/plugin`、`zod`）、
  工具 id 即权限键、`<域>-<动作>` 命名、风险分级登记义务
  （low/medium/high；出现 medium 以上工具时触发 Capability Gateway 建设）

# 3. 增强插件：runtime-ledger（2026-09-05 注册）

把私有 Agent 接上 DPN Runtime。私有模块七个（Phase 9 恢复能力融入 plan/solver 工具，零新增模块），仅 `index.ts` 对外组装。
**工具风险等级：verify-run 为 medium（本地进程执行，Gateway 最小化已触发：
审计 + 权限键 + 登记，`ToolContext.ask()` 审批为第二步），其余全部 low**。

## 事实工具（facts.ts）

| 工具 | 用途 |
|---|---|
| `fact-record` | 记账：unknown/observed/verified/invalidated/staled；source 取调用方 agent 身份；语义缺参返回结构化错误 |
| `fact-query` | 查账：事实状态、置信度、支撑证据（可按 entity/property 过滤） |

## 计划工具（plan.ts）

| 工具 | 用途 |
|---|---|
| `plan-node` | 六动作驱动节点生命周期：created/updated/started/completed/failed/cancelled |
| `plan-query` | 计划视图：节点 + **就绪度推导**（ready/blocked + 精确缺口清单） |

## 执行者工具（solvers.ts）

| 工具 | 用途 |
|---|---|
| `solver-spawn` | 雇佣：原子合同交接（认领 pending+就绪 的节点）；solverId 自动编号在工具层生成 |
| `solver-report` | worker/监督者共用汇报通道：progress/blocked/resumed/succeeded/failed/stopped |
| `solver-query` | 名册 + **shieldContract 合同视图**（合同节点+祖先链+依赖产出+门控事实）+ 履历 |

## 导演工具（director.ts）

| 工具 | 用途 |
|---|---|
| `director-tick` | 控制循环一拍：**纯观察零事件**——planHealth 全景 + 机械建议；判断归教义 |
| `director-review` | 控制结论落账（on-track/needs-replan/needs-reframe + reasoning 必填）——可审计决策档案 |

## 研究工具（research.ts，Phase 10）

| 工具 | 用途 |
|---|---|
| `research-question` | 提出/解决认知问题（Question——unknown 的主动化） |
| `research-hypothesis` | 假设生命周期：proposed（prior+rationale+lineage）→ belief.updated（**必须引用真账证据**）→ evaluated（verdict 阈值转换：supported ≥ 0.8 / refuted ≤ 0.2 / dormant） |
| `research-query` | 认知全景：开放问题（open 优先）+ 假设树（信念/verdict/lineage） |

## 恢复能力（Phase 9，融入 plan-node / solver-report）

- **plan.node.reopened**：failed → pending（retryCount+1、清 owner）——Local Repair 的重启原语；cancelled 不可复活（走 replan 新建）
- **circuit breaker 是制度**：MAX_NODE_RETRIES=3 写死 reducer，超限拒绝并指向 Subgraph Replan（Temporal max attempts 启发）
- **失败分类**：failureClass 六类枚举（tool/environment/permission/logic/knowledge/goal）——归类归智能、校验归制度；solver failure 传播到合同节点
- **supersedes 替代链**：created 声明替代哪个失败节点（校验目标必须 failed）——Subgraph Replan 在图上可见可审计
- **planHealth 透出**：retryableFailures（还有重试预算）与 exhaustedFailures（须 Replan）分开可见
- **失败清理配方**（Saga 补偿的教义化）：stop solver → invalidate 受污染事实 → cancel 下游——原语全有，序列归教义
- **研究对照**：分层恢复四层与蓝图同构、Reflexion=lesson 已有、Reflexion 式重试由宿主驱动（Runtime 无头）

## 记忆工具（memory.ts，Phase 8）

| 工具 | 用途 |
|---|---|
| `memory-record` | 记录泛化经验卡片（lesson）——**锚定强制**：必须引用真账（evidenceId/节点 id/seq），编造被 Runtime 拒绝；或退休被证伪的经验（重排隐藏，从不删除）；返回 relatedExisting 供 agent 显式去重 |
| `memory-recall` | 四类记忆推导视图：lessons（active 先于 retired）+ failures 聚合 + decisions 摘要 + problems（goals/outcomes/已知未知）；**asOf 点时间查询**（按 seq 重放账本后推导） |

## 验证工具（verifiers.ts，Phase 7）

| 工具 | 用途 |
|---|---|
| `verify-run` | **medium 风险**（首个本地进程执行工具）：spawn 真实命令（超时进程树强杀、输出上限 64KB）→ exit code 决定 pass/fail → 自动落账亲证证据（mechanism=verify-run + 指纹）→ JsonlAuditLogger 审计（audit.jsonl）。depth=verified 一步升格 VERIFIED 0.9 |

# 4. DPN-AOR Core Runtime（`src/runtime/`，宿主无关）

纯 TS、零外部依赖、文件背书。核心纪律：**Agent Action → Event →
Reducer → Runtime State**，`dispatch()` 是唯一状态变更入口（落账前
影子试算，坏 payload 零污染），**23 种事件类型**构成系统全部状态变更面。

## Phase 1 事件账本（event-store.ts / reducer.ts / snapshot.ts / index.ts）

- **事件日志**：追加只写 JSONL（权威事实源）；seq 单调递增；并发
  append 串行化；损坏行 fail-loud（包装为 `corrupt event log line`）
- **归约器**：纯函数，无副作用无 IO；未知事件类型/坏 payload/seq 断裂
  一律抛错；输入不被修改（纯度有测试钉死）
- **状态快照**：仅缓存——缺失/损坏/schemaVersion 不匹配一律视为缓存
  未命中，从日志全量重放重建（当前 SCHEMA_VERSION 10，历经 v1→v10 十次
  自动迁移，无迁移脚本）
- **6 种通用事件**：goal.set / task.created / task.updated /
  action.executed / observation.recorded / note.added

## Phase 2 World State & Evidence

- **事实五状态**：UNKNOWN / OBSERVED / VERIFIED / STALE / INVALID
  （蓝图 PARTIAL 暂缓，决策记录在路线图 Phase 2）
- **证据可靠度代码查表**：human 1.0 / test 0.9 / external 0.8 /
  tool_output 0.7 / agent_claim 0.3——agent 只能选类型不能自报分数
- **铁律**：`canVerify` —— agent_claim **永远不足以 VERIFIED**（执行者
  不能证明自己成功），该规则贯穿事实层/计划层/交卷三层
- **置信度推导**：`(status, 证据可靠度)` 纯函数（OBSERVED 封顶 0.6、
  STALE 减半），非申报值
- **迁移矩阵**：非法转换 fail-loud（未观测不能验证、INVALID 复活须新
  观测、验证值必须与观测一致、证伪必须挂证据）
- **5 种事实事件**：fact.unknown / observed / verified / invalidated /
  staled；证据 ID 确定性派生 `ev:${event.id}`，历史证据永久保留

## Phase 4 DPN 基础模型

- **节点五类型**：outcome / milestone / task / verification / recovery
- **生命周期五态**：pending → active → done / failed / cancelled
- **图结构三通道**：`parent`（分解层次，容器先声明，天然无环）、
  `dependsOn`（执行前提，须已存在，环检测拒绝 A→B→A）、
  `preconditionFacts`（世界状态门控——Phase 2 事实成为计划感官）
- **就绪度推导**（`nodeReadiness`）：依赖全 done 且前提事实全 VERIFIED
  才就绪；查询时现算不存储；取消/失败自动阻塞下游（推导涌现，零级联事件）
- **元数据留位**：verifier（凭什么算完）、uncertainty E0–E4（蓝图 §15，
  仅声明逻辑归 Director）、intent（决策上下文）
- **6 种计划事件**：plan.node.created / updated / started / completed /
  failed / cancelled

## Phase 5 Solver Runtime

- **概念**：Solver 是执行者的工牌（合同+履历），本体在宿主侧；三层分工
  = 本体（宿主 agent）← Solver 对象 ← Director（Phase 6 派工）
- **原子合同交接**：spawn 校验节点 pending+就绪 → 节点 active +
  owner=solverId；重复认领拒绝
- **五态**：active ⇄ blocked → finished / failed / stopped（终态不可逆）
- **交卷双侧联动**：success 须非 agent_claim 证据 → solver finished +
  节点自动 completed（同证据登记）；failure → 双双 failed（原因入账）
- **stopped 释放合同**：节点回 pending 清 owner，可重新认领
- **并发铁律**：`MAX_ACTIVE_SOLVERS = 4`，reducer 强制
- **合同推导**（`solverContract`）：合同节点 + parent 祖先链（为什么做）
  + 依赖产出（前面交了什么）+ 门控事实（世界什么样）——上下文从账本算出
- **已知身份缺口**：工具调用按 agent 名归因，solverId 为自报，密码学
  绑定待宿主 session 映射
- **6 种执行者事件**：solver.spawned / progress / blocked / resumed /
  reported / stopped

## Phase 6 Director 控制循环（制度层）

- **Director 不建新对象**：= solver-spawn 认领 outcome 节点 +
  `role: "director"`（蓝图 Primary Solver 原则）
- **planHealth()**：一次返回全局健康全景——完成度（排除 cancelled，比率
  两位舍入）、可派队列、阻塞面（含精确缺口）、活跃工作、卡住名册、失败
  面、已知未知（所有非 VERIFIED 事实键）、churn 计数与比率——全部推导
  非存储
- **churn 由 reducer 独占维护**（created/cancelled 计数）：图翻新率是
  "问题模型可能错了"的先行信号（蓝图 §22）
- **director.reviewed**：五类校验（未知 director / 非 active / 节点与
  合同不匹配 / 合同非 outcome / 缺 reasoning）+ nextActions 格式强制；
  reviews 只增，重启恢复
- **Replan/Reframe 零新事件**：= Director 的判断 + 现有图操作序列，账本
  天然记录每次重排链条
- **1 种导演事件**：director.reviewed

## Phase 8 Memory 系统（3 推导 + 1 原语）

- **设计立场**（融合外部研究：Mem0/Letta/Zep/Claude memory tool）：拒绝
  向量 RAG——记忆天然结构化、embedding 属可吞区；账本与 World State 分别
  同构 Zep 的 episodic/semantic 子图；锚定制是对 Letta "self-editing
  reliability gap" 的制度解法
- **四类记忆三类推导**：Execution=账本、Decision=reviews、
  Failure=failed 聚合、Problem=goals+outcomes+已知未知——`memoryRecall`
  纯函数一次给出；**唯一新存储原语 = lesson（泛化经验卡片）**
- **锚定强制**：lesson 必须引用真账（evidenceId/节点 id 精确校验、
  seq ∈ 1..revision 范围校验、空锚点拒绝）——经验不许编造
- **退休语义**：retired 在 recall 重排中沉底隐藏（分组优先于时间），
  从不物理删除；showRetired 可显式回看
- **asOf 点时间查询**：按指定 seq 重放账本后推导记忆视图（Zep 双时态
  启发，事件溯源免费获得）
- **2 种记忆事件**：memory.lesson.recorded / memory.lesson.retired；
  lesson id 派生 `ls:${event.id}`

## Phase 10 Research Solver（认知对象层）

- **Epistemic State 落地**（蓝图 §2）：Question/Hypothesis 与 World State
  分离——**Fact ≠ Hypothesis**；信念引出而非计算（智能侧自报+rationale，
  制度侧保管与阈值校验），与事实 confidence 的推导制分层不悖
- **五类研究事件**：question.raised/resolved、hypothesis.proposed/
  belief.updated/evaluated（id 派生 `q:`/`h:`）
- **validation gate**：belief 移动必须引用真账 evidenceId（产出制延伸到
  认知层）；**阈值 verdict**：supported ≥ 0.8 / refuted ≤ 0.2 / dormant
  需理由，终态冻结信念；**防泡沫**：refine 未检验 parent 拒绝
- **实验不建新对象**：= 计划节点（可选 tests 声明）+ verify-run 亲证
- **Resolution 桥**：supported 后 agent 显式落 fact.verified——世界状态
  永远由证据产出
- **Lazy Epistemic Expansion 全链闭环**（蓝图 §14）：UNKNOWN 门控 →
  raise → propose → 实验 → belief → verdict → fact.verified → 门开 →
  任务完成
- **研究对照**：与 HEP（arXiv 2607.09195）架构同构且证据机制更强；
  AGM 信念修正印证矛盾显式化路线；MAB 探索利用归智能侧

## Phase 9 Failure Recovery（失败分类、重启与替代）

- **分层恢复对齐**：Local Repair（solver progress 重试 + reopened）→
  Subgraph Replan（cancel + supersedes 替代节点）→ Global Replan /
  Reframe（goal.set + 图重建，director.reviewed 留痕）——后三层是
  Director 的图操作序列，零新原语

## Phase 7 Verification Runtime（证据从申报制到产出制）

- **核心洞察**：证据类型曾由 agent 自选（谎报 test 即得 0.9）。新制度：
  **机器可复现的验证必须机器亲证**
- **裁定规则**：申报的 test/tool_output → 制度降级 external（0.8，
  declared-downgraded，指纹丢弃）——裁定而非拒绝，历史重放与 dispatch
  同构（旧账自动诚实降级，无断代）
- **亲证通道**：verify-run 产出的证据带 mechanism="verify-run" + 命令
  指纹（command/exitCode/timedOut/outputTail）；**零新事件类型**
- **evidence 引用**：observed/verified/completed 可 {evidenceId} 引用已有
  亲证凭证，不重复生成
- **run: 完成门禁**：verifier 以 "run: " 开头 = 机器可读验证声明，
  completed（含 solver success 联动）要求亲证且 command 严格匹配
- **申报收窄**：fact-record/plan-node/solver-report 的证据类型枚举收窄为
  human/external/agent_claim（zod 层堵死申报路径）
- **审计启用**：JsonlAuditLogger 首次真实使用（verify-run 执行记录）
- **run: 门禁补强（E2E 抓出）**：失败的 witness（exit≠0）不能完成节点
  ——"失败的运行不能完成节点"

# 5. 平台共享设施

- **`core/platform.ts`**：`AgentPlatform` 接口——已实现槽位：
  capabilities / **runtime** / state / audit；未实现占位（NotImplemented）：
  execution / permissions / agents / context / memory / verification /
  tasks / models（对应 Phase 7+ 与让渡宿主的部分）
- **`core/capability-registry.ts`**：DefaultCapabilityRegistry（注册冲突
  即抛错；当前无 capability 登记，工具能力由宿主权限键管理）
- **`state/state-store.ts`**：FileStateStore（JSON 读写 + 路径逃逸防护）
- **`state/audit.ts`**：JsonlAuditLogger（结构化审计 JSONL；沙箱归档后
  审计钩子待高风险能力出现时启用）
- **`core/types.ts`**：Capability / AuditEvent / StateStore / NotImplemented

# 6. 教义骨架（留白，等所有者填充）

- **`skills/director-doctrine/`**（渐进披露 + **阶段文件夹隔离**，参照
  v1 M-System）：`SKILL.md` = 总路由器（入口过滤 Fast Path + 四阶段索引 +
  常驻微原则七条）；**每阶段一个自包含文件夹**（阶段间只经 SKILL.md 索引
  衔接、不互引文件）——`planning/` ✅ 自包含（`planning.md` 阶段路由器 +
  `references/plan-graph-schema.md` **通用型计划图 schema dpn-plan-graph
  v2.3（**复合 id + stage 封装 + 演化式网络**——占位节点 provisional 一等化
  （[pending] 前缀 + E3/E4 + verifier 缺省，started 前必补齐）、演化操作
  词汇表（refine/supersede/derive/retire 与 Runtime 原语一一对应）、unknown
  闭环规则；**统一方法论**：四象限为未知密度谱，Director 执行同一演化
  循环不判象限；复合 id + stage 封装——nodes 封装于 stages、id 三段
  {stage}-{step}-{serial}：身份内嵌位置信息、跨阶段引用自携带来源；
  step 为软分组（顺序仍由 dependsOn 声明）、serial 节点序号（串行 1 /
  并行 2,3,4）；mission 身份包 + 按 kind 类型扩展序列化进 intent 零分叉；
  fields 与 plan-node 逐一对齐）+
  `references/plan-translation.md` 机械翻译 + `examples/` Fast Path 单
  节点与复合 DAG 兜底）；supervising/ recovering/ learning/ 待建；
  分解判据待讨论。依据：v1 M-System（作废版）研读 + 用户三主张（决策
  总控/端到端网络/机制结合）
- **状态**：按《初衷.md》交付策略，装备阶段不推进内容填充；规划判断
  全部外置于此（Runtime 只强制留痕，不判断对错）

# 7. Phase 12 收官整合

- **AgentPlatform 诚实化**：接口对齐真实形态（capabilities / runtime /
  state / audit 四槽），8 个假占位移除；让渡项写入接口文档
- **Grand Tour 整合测试**：完整项目生命五幕（立 / 败与修 / 认知插曲 /
  收官与学习 / 时间旅行），15 工具宿主视角一次贯通全链
- **宿主冒烟 v2**（deepseek v4 flash 弱模型三轮）：制度在弱模型压力下
  保持完整——指令漏步被制度拒绝且账本零污染，弱模型准确诊断并拒绝
  越权；研究循环五步全 ok；memory 记录+recall 正常；账本 16 事件无断号
- **调试阶段交接**：装备阶段结束，入口清单见《初衷.md》第二部分

# 8. 测试与验收体系

- **13 个测试文件 / 173 项测试**，`npm run gate` = typecheck + 全链：
  `audit(3) / extensions-routing(8) / registration(10) / runtime-events(16)
  / world-state(17) / plan-model(15) / solver-model(15) / director-model(10)
  / verification-model(18) / memory-model(18) / recovery-model(14)
  / research-model(15) / runtime-ledger(19)`
- **测试方法论**：非法迁移矩阵逐条钉死 + 推导一致性 + 两个数学性质
  （纯度、重放确定性）+ 真实链路全故事（插件 E2E 用真实注册表 +
  临时目录落盘）
- **宿主冒烟已通过**（2026-09-05，真实 opencode CLI 1.18.29）：private
  agent（glm-5.3，`-m` 指定）两轮会话完成 fact 观测/验证 → 事实门控的
  计划节点 → 门未开时雇佣被正确拒绝（账本零污染）→ 门开后雇佣 s1 →
  凭 test 证据交卷节点自动完成。账本 5 事件序列无断号，第二轮会话基于
  第一轮落账事实继续工作（**跨会话重放在真实宿主生效**）。替身测试 +
  宿主冒烟双保险齐备

# 9. 并发模型（多会话收口，2026-09-06）

- **问题**：opencode 多会话/多进程（每次 `run` 独立 server）= 多个 runtime
  实例，各自内存账本缓存互相不可见——seq 撞车导致后写会话被制度锁死
  （响亮失败兜底，数据不腐败但功能锁死）
- **修复三层**（全部收在 runtime 内）：
  1. **文件锁**：写入前获取 `<file>.lock`（O_EXCL）互斥，重试 5s 上限，
     陈旧锁（>30s 崩溃残留）自动打破
  2. **锁内乐观校验**：append 前重读磁盘尾部，缓存过期即重载——seq 永远
     基于权威账本
  3. **dispatch 增量收敛**：返回事件 seq 跳跃 = 他方已写入 → readSince
     增量归约补齐，本实例状态收敛到权威账本（活性语义：每次工具调用都
     自动同步；空闲时 getState 是诚实的时间点快照，refresh() 显式全量同步）
- **读取路径磁盘权威**：readAll/readSince 不再优先内存缓存（多实例下过期）
- **真实宿主验证**：双会话并行 fact-record（deepseek flash）双双落账，
  seq 严格连续；并发单元测试先红后绿（10 条双实例竞态 + 双 facade 收敛）
- **附带发现**：实验同时验证了证据收窄制度的真实并发行为——违规 evidenceType
  被拒后零污染、弱模型准确诊断并拒绝越权

# 9. 数据与运行时位置

- **运行时数据**：`.extensions/state/runtime/` —— `events.jsonl`（账本）
  + `state.json`（快照缓存）+ `audit.jsonl`（verify-run 执行审计）；已 gitignore
- **快照迁移**：schemaVersion 6；旧版快照自动作废、从账本全量重放（Phase 7 存量证据按裁定规则重裁）
- **归档**：`archive/sandbox/` —— Windows AppContainer 沙箱完整实现
  （2026-09-05 归档；OS 级隔离未来由 openchamber 层承担，恢复方法见其
  README）；插件不得引用

# 10. 未实现清单（按《初衷.md》交付策略：装备完成，进入调试阶段）

**装备阶段（Phase 10–12，路线图顺序；Phase 1–9 已完成 ✅）**：
- Phase 10 Research Solver（Problem/Decision/Execution/Failure 四类长期记忆）
- Phase 12 完整 DPN-AOR Runtime 整合
- 近期候选（方向修订）：Fast Path（蓝图 §13 利器化）、两级舰队
  （模型路由，依赖调试阶段实验）

**调试阶段入口清单**（届时按《初衷.md》第二部分执行）：
飞行记录仪（自动证据采集，消费 host event 流）→ 完成门禁 → dogfooding
→ 判断性教义填充 → 两级舰队实验
