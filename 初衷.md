# 初衷 —— DPN-AOR 项目宪法与论纲

> 本文是项目**最高定位文件**。与《DPN-AOR_最新版统一架构蓝图.md》、
> 《agent-platform-plan.md》冲突时，**以本文为准**（overrides 机制，
> 同 agent-platform-plan §0.5 先例）。订立：2026-09-05（2026-09-06 合并
> 论纲与方向修订）。
>
> 结构：第一部分=宪法（定位与不变量）；第二部分=交付策略；第三部分=
> 论纲（论据与推理，2026-09-05 战略讨论沉淀）；第四部分=方向修订；
> 第五部分=文档层级。

------------------------------------------------------------------------

# 第一部分：宪法

## 一句话初衷

> **Harness 不是限制模型的护栏，而是让模型精准攻坚的利器。**

对强模型：不限制或进一步增强其上限。对弱/便宜模型：用时间换下限的
提高和项目的完成度。模型代际更替只改变这个天平的配比，不改变天平本身。

## 三条不变量（任何阶段不得违背）

### 1. 真相层（跨时间）

系统的可信状态必须存在于模型之外、会话之外——事件账本只增不删、
可重放、可审计。模型是租来的智力，账本是自己的真相。
*（对应蓝图 §2 World State、路线图 Phase 1）*

### 2. 证据链（跨声明）

执行者不能证明自己成功。一切"完成"必须锚定非自报证据
（`agent_claim` 永远不足以 VERIFIED）。聪明与诚实是独立变量：
模型换代解决前者，制度锁后者。*（蓝图 §18、Phase 2）*

### 3. 半步分工（两端留人）

AI 接管知识工作的机械中段（检索、起草、执行、验证）；人保留两端——
定目标、担责任。系统设计不得试图吞并这两端：目标由人（或人的教义）
给定，终审归人。*（论纲·论点四）*

## 模型强度适配原则

> **强模型喂状态与感知，弱模型喂纪律与合同。**

- 强模型：账本记忆、planHealth 全局感知、shieldContract 上下文工程、
  教义 skill 私有策略——这些是提上限件，强模型吃了直接变准。
- 弱模型：铁律、就绪门控、并发上限、留痕纪律——这些是提下限件，
  用 harness 的时间换模型的可靠性。
- 终局形态（候选）：**两级舰队**——贵模型当 Director（判断、教义），
  便宜模型当 Solver（纪律下干活）。现有 executor 字段天然支持。

## 非玩具判据

系统每做一次演进，用这条检验：**它优化的是复利的完成度，还是当下的
愉悦？** 聊天记录是玩具态，工程台账是工具态。同样地——

- 系统最大的现实风险不是过时，而是**闲置**：没有真实负载的真理层，
  价值是零。整体调试阶段（见第二部分）必须让至少一个真实任务在账本里
  完整地活一次——最好的候选是系统管理它自己的后续开发（dogfooding）。
- 教义 skill 的价值随运行次数复利、随搁置时间折旧——写出来不保值，
  跑出来才保值。

## 与易腐性的关系

模型每代吞掉 harness 功能清单的一部分。本项目只投资**不可吞四类**：
信任边界、时间、现实锚定、协调。凡宿主平台将提供的（沙箱、基础权限、
通用编排的智能化），主动让渡（先例：沙箱归档 §0.5、Phase 3 撤销）。
判断一件工作是否值得做：先过"不可吞"筛子，再过"非玩具"筛子。

------------------------------------------------------------------------

# 第二部分：交付策略（2026-09-06 决策）

> **先完成所有 Phase，再进行调试——不半途转移目标。先做一个半成品，
> 再逐步优化。**

- Phase 7–12 按路线图顺序完成，中途不插入 dogfooding、教义填充或
  两级舰队实验（它们是**调试阶段**的内容，不是装备阶段的内容）。
- 装备阶段的唯一例外：教义骨架（`skills/director-doctrine/SKILL.md`）
  保持留白等所有者填充，不代笔、不推进。
- 调试阶段的入口清单（届时按此执行，不重新发明）：
  1. 飞行记录仪（自动证据采集插件，消费 host event 流——评估文档 §5.1）
  2. 完成门禁（宣布完成前必须 VERIFIED——评估文档 §5.2）
  3. dogfooding：系统管理自己的后续开发
  4. 判断性教义填充（分解/λ/reframe）→ 最小真活 → 操作性教义从复盘长出
  5. 两级舰队实验（Director=贵模型，Solver=便宜模型）
- 教义与装备是螺旋不是串行：写判断性教义的过程会暴露装备需求；装备
  补齐的判据由教义的执行前提决定（教义说"完成前必须 VERIFIED"，
  没有验证器则教义是空话）。

------------------------------------------------------------------------

# 第三部分：论纲（2026-09-05 战略讨论沉淀）

> 背景：GPT-6 发布引发"强模型是否使本项目多余"的质疑。本部分是推演后
> 形成的共识框架：四条论点、逐条检验与修正、以及对项目的行动推论。

## 论点一：API 形式的模型永远需要 Harness

**命题**：无论模型强到什么地步，只要是 API 形式（无状态函数：上下文进、
token 出），就需要 Harness 增强。

**检验**：成立，但需附加**边界移动定律**——

> Harness 的功能清单不是静态的。每一代更强的模型都会把上一代 Harness
> 的部分功能吸进权重或推理栈（原生工具调用、服务端状态、更长上下文）。
> "harness 永远需要"为真；"今天 harness 做的事永远需要外部 harness 做"为假。

**推论（不可吞的四类职能）**——凡涉及以下四者的，永远留在模型外部：

1. **信任边界**：执行、权限、资金——模型不能给自己授权
2. **时间**：跨会话状态——模型再强也是无状态的，工作跨越周月
3. **现实锚定**：验证——再聪明的模型也不能让挂掉的测试变绿
4. **协调**：多主体名册——个体智力不消除协调成本

对照本项目六层：账本（时间）、证据（现实锚定）、Solver 名册（协调）、
将来 Gateway 的残留职责（信任边界）全部落在不可吞区；通用编排（最易被
吞）只留了制度。项目历史自证：沙箱归档（移交宿主）、Phase 3 撤销（宿主
权限键覆盖）——两次主动放弃易吞区，投对了。

## 论点二：多 Agent 的存亡取决于外置真相层

**命题**：长程任务需要会话管理；多 agent 隔离避免上下文污染；因此多
agent 架构长期存在。

**检验与修正**：单说"隔离防污染"不成立——多 agent 把**上下文污染换成
了上下文损失**（子 agent 丢失父级决策依据）。这正是蓝图 §3 的
Plan–Execution Semantic Gap，本项目的起点。完整命题：

> **多 agent 有价值，当且仅当存在一个跨上下文边界存活的真相层。**

- 隔离 → 防污染（信噪比）
- 外置账本 → 防失忆（跨边界真相）
- 合同推导（shieldContract）→ 给新上下文算出该带的上下文

三者缺一，多 agent 是乘法式失忆。本项目 Phase 1–6 建的正是右半边。

**对强模型长上下文的免疫**：长窗口会消灭**中等任务**的多 agent 需求，
但消灭不了长程工作——污染是信噪比问题不是长度问题；跨越周月的工作超出
任何窗口。

## 论点三：利器，不是护栏

**命题**（一句话初衷的展开）：见宪法。对本项目已建六层做诚实盘点——

| 层 | 提下限（纪律） | 提上限（利器） |
|---|---|---|
| P1 事件账本 | — | ✓ 跨周记忆 |
| P2 事实+证据 | ✓ 铁律防谎 | ✓ 现实锚定 |
| P3 Gateway | 已撤销 | — |
| P4 DPN 计划图 | ✓ 生命周期法 | ✓ 全局结构 |
| P5 Solver | ✓ 并发上限/合同 | ✓ 履历与名册 |
| P6 Director 制度 | ✓ 留痕纪律 | ✓ planHealth 感知 |

结论：护栏件建得快而多，利器件刚起步。**教义 skill 是最大的提上限件**
（私有策略注入，强模型吃了直接变准）；Fast Path（蓝图 §13）是"利器非
护栏"的结构性保险——护栏对一切征税，利器只磨刀刃。

**经济交叉点（诚实的代价核算）**：harness 摩擦（往返、拒绝、重试）对
弱模型是纪律，对强模型可能是枷锁。"用时间换下限"有成本上限，超过交叉
点不如换好一点的模型。推论见方向修订第 2 条。

## 论点四：半步解放，不做玩具

**命题**：AI 是半步解放人类大脑的工具，不应成为玩具。

**检验**：认同，"半步"选得诚实——

- AI 接管知识工作的**机械中段**：检索、起草、执行、验证
- 人保留**两端**：定目标（要什么）、担责任（算不算数）
- "两端留人"不是能力缺口而是责任结构：目标与责任不可外包

**非玩具判据**：玩具优化当下的愉悦；工具优化**复利的完成度**。账本、
证据、档案是"非玩具"的物质基础——它们把聊天变成工程。系统的最大现实
风险不是过时而是**闲置**：一套没人用的真理层，价值是零。

## 行动推论（已并入方向修订）

1. **Fast Path 提前激活**（利器化）：简单任务不进复杂系统——防官僚化
   的结构性保险，蓝图 §13 从"未排期"升为近期候选。
2. **模型路由重新定位为"两级舰队"候选**（蓝图 §16 从"参考数据"挂起态
   激活）：贵模型当 Director（判断、教义），便宜模型当 Solver（在铁律
   纪律下干活）。现有架构的 executor 字段天然支持，无需新原语。
3. **Phase 7 验证器 = 强弱模型的共同地基**：强模型靠它锚定现实（提上限
   的最后一环），弱模型靠它收口完成度（提下限的执行器）。

## 自指声明

本文作者之一是运行在 harness 中的模型（GLM），对本论的认同存在利益
关联；但其日常观察同样来自反面证据——没有外部状态的强模型一样丢工作、
一样需要被钉在证据上。此非纯理论，是运行时经验。

------------------------------------------------------------------------

# 第四部分：方向修订（2026-09-05）

> 本部分为正本；《DPN_AOR_Agent_Runtime_构建路线图.md》中的同名指针节
> 指向这里。

1. **Fast Path 提前激活（利器化）**：蓝图 §13"简单任务不进复杂系统"从
   未排期升为近期候选。这是"利器非护栏"的结构性保险——护栏对一切征税，
   利器只磨刀刃；防止本系统随复杂度增长退化为官僚主义。
2. **模型路由重新定位为"两级舰队"候选**：蓝图 §16 从"参考数据"挂起态
   激活。终局形态：贵模型当 Director（判断、教义），便宜模型当 Solver
   （铁律纪律下干活）。现有 executor 字段天然支持，无需新原语。
3. **Phase 7 验证器定位为强弱模型共同地基**：强模型靠它锚定现实（提
   上限的最后一环），弱模型靠它收口完成度（提下限的执行器）。
4. **每个阶段交付后的强制自问**：它什么时候跑第一次真活？（非玩具判据
   ——闲置是比过时更大的风险。）*（2026-09-06 修订：真活统一推迟至
   调试阶段，见第二部分交付策略。）*

------------------------------------------------------------------------

# 第五部分：文档层级（效力从高到低）

1. **初衷.md**（本文）—— 宪法 + 交付策略 + 论纲 + 方向修订
2. **DPN-AOR_最新版统一架构蓝图.md** —— 架构快照（2026-09-05 版）
3. **DPN_AOR_Agent_Runtime_构建路线图.md** —— 执行路线（含各 Phase 完成记录与方向修订指针）
4. **评估_长程任务能力与平台扩展上限.md** —— 独立评审快照（§5 最小见效包与 §7 平台扩展上限为调试阶段常读项）
5. **agent-platform-plan.md** —— 历史方案（§0.5 之后仅作参考）
6. **架构.md** —— 实现全景（Phase 1–6 全部能力、工具、测试与未实现清单）
