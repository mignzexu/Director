# 评估：DPN-AOR 对长程任务完成度的作用 与 跨平台扩展上限

> 评审日期：2026-09-05
> 性质：纯评估记录，未做任何代码变更。
> 评审范围：`DPN-AOR_最新版统一架构蓝图.md`、`DPN_AOR_Agent_Runtime_构建路线图.md`、`架构.md`、`agent-platform-plan.md`、`.opencode/`（private.md、agent-extensions.ts）、`.extensions/src/`（adapters / runtime / runtime-ledger）。
>
> **状态注记（2026-09-06）**：本文为评审时点快照，正文保留原样。此后变化：
> §2/§4 中"计划仍是文本 / Director 未实现"已过时——Phase 4（DPN）、5（Solver）、
> 6（Director 制度）已建成（见路线图各 Phase 完成块）；§5 最小见效包三件事的
> 现状：③ DPN 最小版 ✅ 已完成，① 自动证据采集（飞行记录仪）= Phase 7 方向，
> ② 完成门禁 = 教义/Phase 7 待做；§7 平台扩展分析与《初衷.md》第三部分
> （论纲）"不可吞四类"结论互相印证（zcode 适配仍为第一候选）。
> **独立性已制度化（2026-09-06）**：ToolPort 三字段收口完成——插件层零
> 宿主 SDK 引用，边界由 boundary.test.ts 锁定；本节"换宿主只需写一个
> adapter"由预言变为现状（MCP 适配 = zod→JSON Schema 转换的纯机械工作）。

------------------------------------------------------------------------

## 1. 结论（TL;DR）

针对"opencode 用户普遍反映长程任务完成度乱、差"这一问题：

- **架构方向对 root causes 是对症的**——外部化状态、Fact ≠ Hypothesis、证据分级验证、计划所有权，正是长程失败的核心解法。
- **但"配置上现有实现"还不能解决问题**——已落地的只有地基（Phase 1–2.5：事件账本 + World State + 证据规则 + 插件管道），而长程失败主因对应的层（计划对象化、全局控制循环、验证门禁）仍在蓝图里，尚未实现。
- **跨平台扩展上限相当高**——runtime core 宿主无关，理论上可达"跨宿主持久世界状态 + 宿主无关编排层"（个人 Agent OS 形态），但被三样东西封顶：宿主 API 面积、模型规划能力、验证的落地性。

------------------------------------------------------------------------

## 2. 根因对照表

先把"长程任务乱"拆成根因，再对照 DPN-AOR 的设计与实现现状：

| 长程失败的根因 | DPN-AOR 对应设计 | 实现现状 |
|---|---|---|
| 上下文腐坏：compaction 丢细节，agent 忘了决策/约束/已做之事 | 事件账本 + World State 外部化状态，快照可重放 | ✅ 已实现（Phase 1–2，测试覆盖） |
| 虚假完成：执行者自证成功，说 done 就 done | 证据分级 + 铁律（agent_claim 永远不足以 VERIFIED） | ⚠️ 规则已实现，但**没有强制点**（见 §4） |
| 计划漂移：agent 跑偏没人拦 | DPN 计划对象化 + Director Gap 循环 | ❌ 未实现（Phase 4/6） |
| 跨会话断片：状态死于会话结束 | 账本追加只写 + 重启重放 | ✅ 已实现且有重启语义测试 |

------------------------------------------------------------------------

## 3. 设计中的三个关键判断

这三个判断直击行业痛点，是本系统最有价值的部分：

1. **Fact ≠ Hypothesis**（World State 与 Epistemic State 分离）——认知与现实分离，"相信"不进入事实账本。
2. **执行者不能证明自己成功**——`canVerify` 铁律：agent_claim 可靠度 0.3，永远不足以将事实推到 VERIFIED；验证必须来自 test / tool_output / human / external 证据。这是"agent 说 done 就 done"的病根解法。
3. **Plan Ownership = Execution Ownership**——对 Plan–Execution Semantic Gap 的诊断正确：规划者必须持续拥有计划的认知所有权，局部智能分布、全局意图集中。

------------------------------------------------------------------------

## 4. 现状差距分析（基于代码证据）

差距在"强制点"，不在"基础设施"：

1. **记账是自愿的。**
   - `.opencode/agents/private.md` 通篇未提及 `fact-record` / `fact-query`；
   - L1 入口已把 opencode 的 event 流接进插件管道（`.opencode/plugins/agent-extensions.ts` 的 `event` hook），但 L2 注册表中没有任何插件消费它（`adapters/opencode/index.ts` 仅注册 runtime-ledger，其工具只在被调用时才写账）；
   - 结论：账本目前是一台**没有司机的发动机**——而长会话里模型自觉性恰恰是最不可靠的东西。
2. **没有完成门禁。** agent 今天依然可以宣布 done 而不查账；铁律只在"有人来记账"时才生效。
3. **计划仍是文本。** DPN（Phase 4）未实现，漂移问题的解还不存在；Director 循环（Phase 6）未实现，全程 Gap 计算还不存在。

------------------------------------------------------------------------

## 5. 最小见效包（仅作未来路线参考，本次不实施）

要"配置上就见效"，最小集是三件事，均不违背现有架构：

1. **自动证据采集（飞行记录仪）**：新增一个消费 host event 流的增强插件，把 bash 执行结果、测试运行、文件编辑自动记为 `tool_output` 证据——不依赖模型自觉。管道已接好，只差插件。
2. **完成门禁**：先在 `private.md` 政策中写死"宣布完成前必须 `fact-query` 确认关键事实为 VERIFIED"；后续升级为 verification 插件（Phase 7 最小版）。
3. **DPN 最小版**：哪怕只是 milestone/task 节点的结构化 checklist 对象进 Runtime，让计划可检查、可质疑，而不是一段会腐烂的文本。

------------------------------------------------------------------------

## 6. 系统救不了的三件事

诚实边界，配置不能替代的东西：

1. **证据权重是启发式不是真理。** test 0.9 / tool_output 0.7 等可靠度是人设权重；"测试通过 ≠ 需求被满足"。
2. **goal → 可检查 postcondition 的翻译本质仍是 LLM 判断问题。** 目标模糊时，验证体系再严也只能严格地验证一个错的目标。
3. **模型能力与成本。** 底层模型规划能力弱时，结构能防漂移但不能点石成金；多一层循环 = 多 token 多延迟——Fast Path 的设计意识正确，应保持。

------------------------------------------------------------------------

## 7. 平台扩展上限三档分析

可移植性的关键决定：**L1 唯一入口 → L2 静态注册表 → 自包含插件 → 宿主无关 runtime core**。runtime（event store / reducer / world state）是纯 TS、文件背书、零宿主依赖；换宿主只需写一个 adapter。

### 下限（容易，且已是差异化能力）

runtime 包成 MCP server / zcode 插件，`fact-record` / `fact-query` 在任何宿主可用。得到**跨宿主持久世界状态**：opencode 开工、zcode 续作，任务事实不死于会话。目前主流工具均无此能力。

### 中段（Phase 4–7 建成后）

runtime 独立进程化，成为宿主无关控制面；Director 驱动任意宿主的 agent，宿主降格为"执行器 + UI"。接近"个人 Agent OS"形态——原则上**超过任何单一宿主的 native 长程能力**，因为它外部化、可审计、宿主无关。

### 天花板（被什么压住）

1. **宿主 API 面积是总闸门。** 能力网关、沙箱、循环控制的深度取决于宿主给不给钩子（zcode 有 permission modes / hooks，opencode 有 plugin API，更弱的宿主可能只有 MCP）。宿主不给强制点的地方，enforcement 退化为 convention——账本能**记录**违规但不能**阻止**违规。
2. **模型本身。** 结构防漂移，不提升智能。
3. **验证落地性。** 换平台解决不了 goal→postcondition 的翻译难题。
4. **每宿主 adapter 维护成本。** 宿主 API 变化时 L1 要跟着改；薄入口设计已把该成本压到单文件，但不为零。

------------------------------------------------------------------------

## 8. 评审元信息

- 结论状态：仅评估；**未修改、未删除任何现有文件，未做任何代码变更**。
- 后续决策入口：§5 最小见效包（见效最短路径）、路线图 Phase 4（DPN）、§7 平台扩展（zcode 适配为第一候选）。
